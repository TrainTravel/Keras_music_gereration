# Core (markov engine + MIDI rendering + offline WAV synth)
mido>=1.3
numpy>=1.21

# Development
pytest>=7

# Optional: only needed for --engine lstm
# tensorflow>=2.16

# Optional: only needed for --video (MP4 rendering)
# imageio-ffmpeg>=0.4
